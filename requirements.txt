# Academic Debate Council - Dependencies for Deployment

# Core dependencies
chainlit>=2.0.0
anthropic>=0.18.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Optional (already included in Chainlit)
# fastapi>=0.104.0
# uvicorn>=0.24.0
